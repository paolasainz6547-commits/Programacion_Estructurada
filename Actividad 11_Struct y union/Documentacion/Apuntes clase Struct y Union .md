# Struct y Union
-  (Extraido desde Notion)

En C se indica el numero de filas y columnas para calcular correctamente el espacio de memoria

## Struct y Union en C

Un struct permite agrupar varios tipos de datos bajo un mismo nombre

Definir un struct es como definir un tipo de dato, se definen arriba de una funcion 

struct alumno, entonces:

alumno es el tipo de dato

Union agrupa datos distintos, pero que comparten la misma memoria 

utilizar y reservar en memoria solamente  

un solo tipo de dato, su tamaño será  tipo de dato el que sea mas grande
